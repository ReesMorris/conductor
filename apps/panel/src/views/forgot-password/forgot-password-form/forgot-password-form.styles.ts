import { css } from '@/styled-system/css';

export const styles = {
  form: css({
    display: 'flex',
    flexDirection: 'column',
    gap: '4'
  }),
  successMessage: css({
    textStyle: 'sm',
    color: 'foreground.subtle'
  })
};
