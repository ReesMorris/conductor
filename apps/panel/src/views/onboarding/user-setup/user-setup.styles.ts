import { css } from '@/styled-system/css';

export const styles = {
  form: css({
    display: 'flex',
    flexDirection: 'column',
    gap: '4'
  }),
  submitButton: css({
    display: 'flex',
    marginInlineStart: 'auto',
    marginBlockStart: '4'
  })
};
