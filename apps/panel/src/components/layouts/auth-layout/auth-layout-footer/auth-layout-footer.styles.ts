import { css } from '@/styled-system/css';

export const styles = {
  footer: css({
    marginBlockStart: '8',
    textAlign: 'center',
    paddingInline: '4'
  }),
  footerText: css({
    textStyle: 'sm',
    color: 'foreground/40'
  })
};
