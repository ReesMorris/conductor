import { css } from '@/styled-system/css';

export const styles = {
  actions: css({
    padding: '6',
    inlineSize: 'full',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'flex-end',
    marginInline: 'auto',
    gap: '4'
  })
};
