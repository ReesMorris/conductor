import { css } from '@/styled-system/css';

export const styles = {
  root: css({
    display: 'flex',
    flexDirection: 'column',
    gap: '0'
  }),
  list: css({
    counterReset: 'step-counter'
  })
};
