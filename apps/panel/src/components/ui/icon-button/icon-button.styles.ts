import { css } from '@/styled-system/css';

export const styles = {
  iconButton: css({
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    aspectRatio: 'square',
    paddingInline: '0'
  })
};
