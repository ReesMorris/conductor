import { css } from '@/styled-system/css';

export const styles = {
  contentContainer: css({
    minBlockSize: '64'
  }),
  closeButton: css({
    position: 'absolute',
    insetBlockStart: '2',
    insetInlineEnd: '2'
  })
};
