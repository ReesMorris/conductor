import { css } from '@/styled-system/css';

export const styles = {
  container: css({
    display: 'flex',
    gap: '3',
    inlineSize: 'full',
    alignItems: 'center',
    justifyContent: 'space-between',
    marginBlockStart: '6'
  })
};
