import { css } from '@/styled-system/css';

export const styles = {
  header: css({
    display: 'flex',
    flexDirection: 'column',
    textAlign: 'center',
    paddingBlockStart: '2',
    marginBlockEnd: '2'
  })
};
