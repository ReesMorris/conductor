import { css } from '@/styled-system/css';

export const styles = {
  container: css({
    flexShrink: '0',
    display: 'flex',
    flexDirection: 'column',
    inlineSize: 'sidebar',
    padding: '6',
    gap: '6'
  })
};
