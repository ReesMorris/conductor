import { css } from '@/styled-system/css';

export const styles = {
  select: css({
    blockSize: 'ui.md',
    inlineSize: '48',
    // backgroundColor: 'background.elevated',
    borderRadius: 'md',
    paddingInline: '2'
  })
};
