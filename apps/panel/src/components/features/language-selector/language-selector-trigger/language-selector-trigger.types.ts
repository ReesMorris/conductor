export interface LanguageSelectorTriggerProps {
  /**
   * Indicates whether the language selector is currently open.
   */
  isOpen?: boolean;

  /**
   * The native name of the currently selected language.
   */
  currentLanguageName?: string;
}
