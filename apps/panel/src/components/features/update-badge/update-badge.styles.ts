import { css } from '@/styled-system/css';

export const styles = {
  button: css({
    cursor: 'pointer',
    display: 'inline-flex',
    alignItems: 'center'
  }),
  icon: css({
    display: 'inline-flex',
    verticalAlign: 'sub'
  })
};
