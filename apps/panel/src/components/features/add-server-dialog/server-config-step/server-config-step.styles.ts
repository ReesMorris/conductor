import { css } from '@/styled-system/css';

export const styles = {
  container: css({
    display: 'flex',
    flexDirection: 'column',
    gap: '5'
  }),
  connectionTypeGrid: css({
    display: 'flex',
    flexDirection: 'column',
    gap: '3'
  })
};
